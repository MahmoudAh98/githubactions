name: First Workflow
on:
  push:
    branches:
      - main

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello world
        run: echo "hello world"
      - name: checkout repo
        uses: actions/checkout@v6.1.0
      - name: run command
        run: ls -la

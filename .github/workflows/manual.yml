on: 
  workflow_dispatch:
    inputs:
       environment: 
         description: "choose environent"
         required: true
         default: staging
         type: choice
         options:
           - staging
           - production
   
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Application
        run: echo "Deploying Aplication to ${{ inputs.environment }}"
        
